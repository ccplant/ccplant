# v0.3.5 deployment

Deployed the `v0.3.5` backend and frontend Helm charts on 2026-08-02.

| Environment | Namespace | Backend revision | Frontend revision | Verification |
| --- | --- | ---: | ---: | --- |
| Development | `agentapi-ui-dev` | 1233 | 646 | UI returned HTTP 307; API `/health` returned HTTP 200 |
| Production | `agentapi-ui` | 653 | 329 | UI returned HTTP 307; API `/health` returned HTTP 200 |

The HTTP 307 responses are the expected authentication redirects for the UI.
